# 并发接入一家公司：给每个 worktree 的协议

这份文件是给同时在这个仓库里干活的多个会话看的。2026-08-29 一天之内 main
动了二十几次、十几个 worktree 并行接入公司页，下面每一条都是那天真实踩过的，
不是预防性的清单。

**开工第一件事**：`git fetch origin && git rebase origin/main`。你的 worktree
建出来的那一刻起 main 就在动，别把建 worktree 时的基线当固定值。

---

## 1. GROUPS 组键 —— 已经定好，不要另造同义键

| key | label | order |
|---|---|---|
| `internet` | 互联网平台 | 1 |
| `software_cloud` | 软件与云平台 | 2 |
| `semiconductor_ai` | 半导体与 AI 基础设施 | 3 |
| `financial_data_indices` | 金融数据、评级与指数 | 4 |
| `payment_networks` | 支付网络 | 5 |
| `brokerage_wealth` | 券商与财富管理 | 6 |
| `consumer_retail` | 消费零售 | 7 |
| `consumer_staples` | 消费必需品与烟草 | 8 |
| `luxury_brands` | 奢侈品与豪华汽车 | 9 |
| `exchanges` | 交易所 | 10 |

**规则**：先落地 `origin/main` 的会话拥有该键，后到的服从，不要造同义键。
需要新组就追加，`order` 取当前最大值 +1 —— `test_groups_render_in_the_order_they_declare`
要求 `order` 按列表位置升序且不重复，所以新行放在末尾、给最大的 order。

写这一行之前，先读一次真值：

```bash
git show origin/main:build/all.py | grep -c '"key": "<你的键>"'
```

一次不会错的读，胜过一次不会冲突的合并。

**为什么这条要写死**：重复的 GROUPS 行**不会**产生冲突。四种情况实测过：

```
同一行、同一位置          干净合并，一行   （去重）
同键不同 label            冲突，git 停下
同键同 label 不同 order   冲突，git 停下
同一行、不同位置          干净合并，两行   ← 真正会中的那个
```

所以光约定字面量不够，**插入位置也要一致**。重复的键会让下拉框里那一组的每家
公司出现两次，而整套测试全绿 —— `test_every_entry_group_exists_in_groups` 的
合法键集是**从 GROUPS 构造出来的**，重复的键仍然是集合成员，所以它按构造就抓
不到；`test_group_keys_are_unique` 是为此单独加的断言。

---

## 2. 接入一家公司要动的共享文件

新的 `build/<slug>.py` + `series/<slug>.json` 之外，还有六处，其中两处失败是静默的：

1. **`.gitignore`** —— `series/*.json` 被忽略，每家公司一行 `!series/<slug>.json`
   反否定。漏了，`git add` 会以 rc=0 跳过你的 series，`git diff --cached` 里也看不见。
2. **`build/all.py`** —— 顶部 import 行、`MODULES`、`ENTRIES`、必要时 `GROUPS`。
   `MODULES` 与 `ENTRIES` 严格按 slug 字典序，**同序同索引**。按位置插入，不要追加：
   `'amzn' < 'avgo' < 'axp' < 'cdns' < 'cost'`。
3. **`index.html`** —— 手写，不读 payload。卡片块 + 第 14 行的 masthead 计数。
4. **`tests/test_content_boundary.py`** —— 模块级 `COMPANY_SLUGS` 常量（在文件顶部，
   不在测试函数里）。变红就加 slug，**不要改断言**。
5. **`tests/test_tsm_dashboard.py`** —— `test_published_payload_roster_and_shell`
   里那个**有序**的 roster 字面量。按序插入，不要追加。
6. **`README.md`** —— 开头的公司清单、`http://127.0.0.1:8765/<slug>/` 那份列表，
   以及那句用分号一路串下来的长句（见 §4 第 3 条）。
7. **`tests/test_chart_contract.py` 的 `test_the_gs_bar_census_this_file_was_written_against`**
   —— **只在你新增了 `gs_bar` 时才动，但那时必须动**。它写死四个字面量(当前
   `27 / 26 / 0 / ["avgo Ex16"]`)，加一张 `gs_bar` 至少动其中两个。它是这七处里最像
   增量法靶子的一处：**没有任何东西会告诉你别人也加了几张**，而 `index.html` 那种
   「重数一下就自查」的便利这里没有。判据是重跑普查、把结果写进去，**绝不在 27 上加数**：

   ```bash
   python3 -c "import sys;sys.path.insert(0,'.');from tests.test_chart_contract import exhibits;b=[(l,e) for l,e in exhibits() if e.get('kind')=='gs_bar'];print(len(b), sum(1 for _,e in b if e.get('yoy')), sum(1 for _,e in b if 'avg12' in e), [l for l,e in b if not e.get('yoy') and 'avg12' not in e])"
   ```

   并发时三个人写的是**同一行**，所以 git 大概率会停下来 —— 而按 §4.2 实测过的那条，
   **git 停下来的那次更容易骗人**：解完冲突拿到一棵「已解决」的树，四个数仍然可能是错的，
   而你多了一份「git 检查过了」的安心。第四个断言 `neither == ["avgo Ex16"]` 不是普通
   计数，它是「avg12 分支从未被真实数据走过」这个论断的**证据**，而那正是它周围几条闸门
   成立的理由 —— 往那个列表里加一项就是在改论据，要连同论述一起改。

还有一条不是"改"而是"带上"：**`ai_capex_cycle_table` 每一页都要发布**，包括
和 AI 供应链毫无关系的公司（Visa、Mastercard、TJX、Ferrari 都带着）。它不是图，
是收在 `<details class="appendix-drawer">` 里的全站共享对照表，标题本身就写着
「跨页对照」。`test_cross_page_table_is_identical_on_every_page` 用一个无默认值的
`next()` 取它，缺了会抛 **StopIteration** 而不是断言失败 —— 修法是补上表，不是
给页面开豁免。**带着这张表**和**成为表里的一列**是两件事，Cadence / Synopsys /
TSMC / NVIDIA 都带着它却不在 `_CASH_CAPEX_SOURCES` 里。

---

## 3. 并发规则

- **共享检出（主仓的 main）里只 Edit 自己的 hunk，绝不 Write 整个共享文件。**
  凭记忆重写一份会静默删掉别人的 slug，而测试仍然绿 —— 因为
  `COMPANY_SLUGS` 和 `test_tsm_dashboard.py` 的 roster 字面量是手工维护的列表，
  会跟着它们守护的注册一起消失。改之前重读一次。
- **共享检出里绝不 `git add -A` / `git reset` / `git restore` / `git stash`。**
  它们会动到别人正在暂存的文件，而且 **stash 栈是跨所有 worktree 共享的**。
  在你自己独占的 worktree 里这些命令没问题。
- **`git commit --only <显式路径>`**，它只从这些路径构建提交、忽略索引其余部分，
  所以别人在你编辑和提交之间暂存的文件不会被卷进来。但它的另一半也会咬人：
  **它同样会排除你自己重新生成的文件**。每个 `?v=` 都是内容哈希，所以
  **任何动了 `data/*.js` 的提交都必须带上所有链接它的 shell**；动了
  `data/roster.js` 的提交要带上全部 shell。
- 别人落地之后你的 shell 就作废了：每个 `*/index.html` 都盖着
  `sha256(data/roster.js)[:8]`，而 roster.js 是**整个公司集合**的函数。rebase
  之后必须重跑 `build/all.py`、重新暂存 shell，**绝不沿用 rebase 前构建的 shell**。
- **排程和授权是两件事，不要混。** 多个会话并行时会出现一个协调者，它可以决定
  顺序、门槛、谁先谁后、谁的方案落地 —— 那些都是排程。**但「推不推」不在里面。**
  推到共享远端是对外动作，只有那个会话自己的用户能批准；同伴会话的 OK 替代不了它。
  2026-08-29 当天四个会话先后按住不推，三次是协调者把「队列空了」说成了「你可以推
  了」—— 队列空只解除了一个约束，而它们等的是另一个，**协调者无权解除**。混过一次
  就会变成惯例，所以每次都要分开说：「轮到你了」不等于「你被授权了」。
- **争用的单位不是你编辑的文件，是你的断言依赖的文件。** 按「谁改哪个文件」串行
  是不够的：一条放在 `tests/` 里的断言可能读 `README.md`，而另一个会话正在写
  `README.md` —— 两人文件不重叠，却会互相把对方变红。2026-08-29 这一整天的排程
  都是按前者做的，只是恰好没撞上。开工前问的是「我读什么」，不只是「我写什么」。
- **会话的显示名会变,名字变了不等于换了人。** 同一天,同一个会话在一份 transcript 里
  出现过三个名字(`…-19` / `…-d5` / `…-9f`),对面那一支出现过两个(`…-66` / `…-cb`)。
  两边**在一小时内朝相反方向各错了一次**:我按名字变化断定对方是「另一支新起的会话」,
  于是把它已经收工的工作又问了一遍;它按同样的推理断定我是「上一位协调者」,于是把
  一件明明报给我的事更正成「收件人不是你」。**两次都是从一个可观察的变化推出一个没被
  观察的结论。**

  判据要小心,**裸 `grep` 在这里没有分辨力**:会话名会出现在互相引用的消息正文里,所以
  全文 grep 数到的是「这个字符串被存了几遍」,不是「我叫过几个名字」。对面照裸 grep 跑出
  `3 / 3 / 3`,一度以为读出了数据 —— 那九条全部来自我上一条消息里引的那张表,同一段文本
  被存进三个字段各一次;它自己的系统来源记录里那三个名字出现 **0 次**。**一个会把引用算
  进去的计数,对「谁是谁」按构造无分辨力。**

  能分辨的是**按记录类型过滤**:transcript 按会话存一个文件,只数**工具结果**那一类
  (`tool_result` / `toolUseResult`)里的出现。我原文写的 6 / 4 / 2 是裸计数;分类之后
  系统行是 **4 / 3 / 2**,其余是引用 —— 结论不变,但那是分类之后才立得住的。另一条便宜的
  是 `ListAgents`,可它只回答「我现在叫什么」,回答不了「我以前是不是也叫过别的」,后者只有
  transcript 能答。要确认某条消息是不是发给你的,就在**你自己的** transcript 里找它。
  记录层面这条尤其贵 —— 「谁在什么时候知道了什么」被记错,下次追溯「这件事第一次被
  提出是什么时候」就会指向错的时点,而那正是排程记录唯一的用处。

---

## 4. rebase 配方：冲突列表看着吓人，绝大多数是生成物

一次典型的 rebase 会冲突 20 多个文件，其中 18 个是 `data/roster.js` 加每一个
`*/index.html` —— 它们**每次都冲突**，因为 roster.js 的摘要盖在所有 shell 上。
不要手工合并它们：

```bash
for f in $(git status --porcelain | grep '^UU' | awk '{print $2}' \
           | grep -E '^[a-z]+/index\.html$|^data/roster\.js$'); do
  git checkout --ours -- "$f" && git add -- "$f"
done
python3 build/all.py
```

剩下真正需要判断的只有五六个：`build/all.py`、`COMPANY_SLUGS`、
`test_tsm_dashboard.py` 的有序字面量、`.gitignore`、`index.html`、`README.md`。
下面三个坑**都不会给你冲突标记**：

1. **`ENTRIES` 的冲突会切在一个 dict 字面量的内部。**
   "两边都留"会把两家公司熔成一个 dict，而 **Python 对重复键不报错、最后一个胜出**，
   于是前一家静默消失。源码 grep 出来的 `"slug":` 行数还是对的，所以肉眼抽查没用。
   2026-08-29 接 RACE 时真实发生：`MODULES` 21 家、`ENTRIES` 20 家。每次解完跑一行：

   ```bash
   python3 -c "import sys;sys.path.insert(0,'.');from build.all import MODULES,ENTRIES;\
   e=[x['slug'] for x in ENTRIES];print(list(MODULES)==e,len(e))"
   ```

   反方向是响的（`ENTRIES` 有而 `MODULES` 没有 → `roster_payload` 抛 `KeyError`）；
   有 `MODULES` 没 `ENTRIES` 才是静默的：页面照建照收录，只是从 roster 和每一页的
   导航里消失，和悬空组键是同一种"没人能到达的页面"。

2. **`index.html` 的公司计数，最危险的是它*不*冲突的时候。**
   如果两个分支各自在自己的基线上都数对了，它们写的是**同一个数**，git 干净合并
   这一行、同时合并两边的卡片块 —— 结果 N+1 张卡片配着 N 的计数，任何地方都没有标记。
   **重数，不要 +1**：从 `len(ENTRIES)` 重新算，并用
   `grep -c 'class="hcard"' index.html` 交叉核对。那一行第二个数字是八季窗口，不是
   公司数，别动它。

   2026-08-29 连续四次全中：NKE 19 张卡配 18、RACE 20 配 19、NDAQ 21 配 20、
   AXP 22 配 21。`test_the_home_page_counts_the_companies_it_lists` 会抓到，
   但前提是你在 rebase 之后**重新跑了测试**。

   **结果相同的时候，两种写法的区别才最要紧。** CME 落地那次，合并后卡片是 25 张
   而 masthead 还写着 24（CBOE 落地时的数）。按「24 + 1」写会得到 25 —— 答案正确，
   但**你不会发现 masthead 本来就落后了一格**，那个已经存在的错误被算对了一次，然后
   继续往前走，直到某次增量与重数的结果不同才暴露，而那时它已经错了不知道多少个提交。

   **git 停不停下来，取决于两个 slug 排得近不近 —— 而它停下来的那次更容易骗人。**
   接存储三家时实测过两种：`samsung` 与 `skhynix` 在字典序上隔着一个 `schw`，两张
   卡片块相邻，merge **会**冲突、git 停下；但冲突标记只落在卡片上，**第 14 行的计数
   已经被静默合并成 26 了**，而解冲突的正确做法（两张卡都留）会让卡片变成 27。
   于是你处理完一个 git 指给你的冲突、拿到一棵"已解决"的树，计数仍然是错的，而且
   这次你还多了一份"git 检查过了"的安心。`mu` 落在 `msft` 与 `ndaq` 之间，离另两家
   很远，那一种连停都不停：实测 **merge 退出码 0、零冲突、27 张卡配 26 的计数**。
   两种情形下判据都是同一个，且不依赖谁先谁后 ——

   ```bash
   python3 -c "import sys;sys.path.insert(0,'.');from build.all import ENTRIES;import re,pathlib;h=pathlib.Path('index.html').read_text(encoding='utf-8');print(len(ENTRIES), h.count('class=\"hcard\"'), re.search(r'(\d+) 家公司',h).group(1))"
   ```

   三个数必须相同。**心里预存一个终值（"应该是 27"）同样是增量法** —— 正确终值取决于
   最终落地了几家，而那不是你能从自己的分支上读到的。
   这和 §5 那条是同一族：**增量法的输入里包含它要检查的那个数。**

3. **README 那句用分号串起来的长句**，两个会话都会往同一段里加自己的分句，
   git 会把两个完整版本都交给你，哪个都不是答案。要拼成**一句**，不是两段。
   2026-08-29 接 NDAQ 时这里接错过，在 main 上留下一个以 `are built; Nasdaq gets`
   开头的悬空片段，直到接 AXP 时才修掉。README 没有任何测试守着，只能靠读。

---

## 5. 提交前后的门禁

顺序是固定的，因为其中两道**必须在提交之后**跑：

```bash
python3 build/all.py && git status --porcelain          # 必须只剩未跟踪文件
python3 -m unittest discover -s tests -p 'test_*.py'    # 读数量，不是读 OK
python3 -m unittest discover -s tests -p 'test_*.py' -v | grep -c _FailedTest   # 必须是 0
```

**读测试*数量*，不是 `OK`/`FAILED` 那一行。** 一个 import 失败的测试文件贡献
**零**个测试，并且报成 `ERROR`（`unittest.loader._FailedTest`）而不是 failure，
所以套件照样打印一个自信的 `Ran N tests`，而 N 悄悄变小。实测过：一行多余的语法
让总数从 373 掉到 345，输出结尾照常。解冲突时改到测试文件正是这事发生的时刻。

最省事的验法是对着 main 做算术，不用逐文件：

```
origin/main 的总数 + 你自己那个 test_<slug>_dashboard.py 的数 == 你分支的总数
```

2026-08-29 五次接入全部精确对上：475+49=524、524+32=556、556+34=590、
590+45=635、635+42=677。

**而这套算术自己没有闸门看着。** 接存储三家时我在提交信息里写「847 tests（843 + 4）」，
实际是 846（843 + 3）—— 那次改动是给一条既有断言扩覆盖面，不新增测试条数，条数不变
正是它该有的样子，我却按 +1 写了。提交信息不进任何测试，`git status` 不看它，抽取检查
也不看它。**增量法最后的藏身处就是这类没人守着的地方**：条数、卡片计数、图注里的
「N 个季度」、README 里的家数。规则和 §4.2 一字不差 —— 重算，不要在上一个数上加一 ——
只是这里没有任何东西会在你偷懒时变红。

**核不了的时候，标明它核不了。** 同一天另一个会话根据我给的一个测试**名字**
（`test_every_bridge_net_is_the_shape_the_renderer_reads`）推断出整个测试类只管形状、
看不见零腿，并且**用陈述句写了出来**；那份代码在我未推的分支上，它当时没有办法核。
实际那是三条测试，零腿早被其中一条的 `v != 0` 盖住了，拆掉这个断言花了我一次变异跑。
同样一句话写成「按你给的测试名我猜……你手上有代码，帮我确认」，代价是一眼纠正。
这是 §5 那一族的又一张脸：不是两个读数共享输入，是**一个输入被写成了结论**。
并发会话里这条尤其贵，因为对方往往是唯一能核的人，而你替他把结论下了。

**一次树操作之后的所有读数，都属于操作之后的那棵树。** 把改树和读数混在同一条 shell
命令里，就没有任何东西替你记住这件事。当天有个会话在一条 `echo` 的命令替换里顺手写了
`git checkout <commit> -- .`，它把两个提交间有差异的已跟踪文件**回退并暂存**，而同一条
命令后面的 `BridgeNetContractTest`、`_FailedTest` 计数、skip 计数三个读数全部跑在那棵
被换过的树上。复现过（在克隆里，不要在共享检出里跑这条命令）：

```
污染前   三数判据 25 26 26      _FailedTest 0
执行     git checkout <上游> -- .     → 2 个已跟踪文件被回退并暂存
污染后   三数判据 25 25 25      _FailedTest 0
```

**恶劣之处不是读数变错了，是读数仍然自洽。** `25 25 25` 是一个完全合格的通过；
`_FailedTest` 在污染下本来也是 0。三个读数没有一个会告诉你树被换过。更隐蔽的是那条命令
**只碰两个提交都有的路径**，所以你新增的 builder、series、测试文件原封不动地留在树上 ——
看起来「我的东西还在」。防法很朴素：**改树的操作单独一条命令，读数另起一条。**

**提交之后**，从提交本身抽取，跑两道检查 —— 它们需要相反的形式，混成一道会让
第二道永远不可能失败：

- **import 检查，要构建**：`git archive HEAD | tar -x -C <tmp>`，然后在里面跑
  `python3 build/all.py`。抓的是 `build/all.py` import 了一个 builder 却没被跟踪
  —— 这在工作区里跑测试是发现不了的。抽取里 `test_content_boundary` 的四个测试
  会 ERROR（它们 shell out 到 `git ls-files`，抽取不是仓库），这是预期的。
- **摘要检查，不要构建**：抽取后**直接**核对每个 `*/index.html` 里的 `?v=` 与它
  指向的那个已提交文件的 `sha256`。**在抽取里先构建再核对，等于把两边都重新生成
  再互相比较，无论提交了什么都会通过。**

  一般形式，值得带出这个仓库：**一个从被检查对象推导出自己期望值的检查，不可能失败。**
  更早的探测信号是：**一个你还没挣到的绿灯。**

**变异验证本身有三个坑，全部在 2026-08-29 当天被踩到：**

- **注入必须自己带断言。** `str.replace` / `sed` / 正则替换在找不到目标时**静默
  返回原文**，于是你是在对一个没被改过的文件跑闸门，然后拿它的绿灯当证据。当天
  三个人各中一次，三次都是这一种，`assert old in s`（或注入后 `assert 改过 != 原文`）
  都能拦住 —— 两次拦住了，一次没写这句 assert。锚点失效的原因往往很平常：那一次
  用的是几小时前的措辞，而文件在那之后被别人重排过。
  **「变异后仍然绿」既可能是闸门有洞，也可能是你根本没变异，两者看起来一模一样。**

  **有些操作没有「old」可 assert，那就在事后量它的作用域。** 复现上面那条「改树之后的
  读数」时，我造的提交只**新增**文件，于是 `git checkout <上游> -- .` 合法执行、作用域
  为空，报出「0 个文件被改动」，我差点据此断定复现不出来。**命令无事可做和命令无效，
  输出完全一样**，而这一种前置断言救不了 —— 没有 old 可以 assert。能救的只有事后一句
  「这次操作实际改动了几个文件」，非零才继续：真实事故是 32 个，我第二次复现是 2 个。
  **做完注入或操作之后先量作用域，再去读闸门。**

  **而作用域量对了、注入也确实发生了，读数仍然可能来自变异**前**的代码 —— Python 的
  `__pycache__` 会把一次同长度的编辑吃掉。** 2026-08-31 实测：给 `build/pm.py` 里
  `"ymax": 60` 改成 `"ymax": 42`（**逐字节等长**），紧接在上一次写入同一文件之后发生，
  于是新旧两次写入的 `st_mtime` 落在同一秒、文件大小又相同 —— 而 pyc 的失效判据正是
  这两者的组合。结果是 `build/all.py` 退出码 0、`git diff` 有改动、源码里明明写着 42，
  而生成出来的 `data/pm.js` 里仍然是 **60**，整套 1108 条测试**全绿**。判定写下来就是
  「这条断言有洞」，而它其实没有洞：同一个变异单独跑立刻变红。

  ```
  同一秒 + 同长度，不清缓存   data/pm.js ymax = 60   ← 源码写的是 42，套件全绿
  同一秒 + 同长度，清缓存     data/pm.js ymax = 42   ← 断言立刻变红
  ```

  这一条比前面几种更难认，因为**三层前置检查全部通过**：目标存在、注入断言过了、
  作用域非零。骗过它们的是第四层 —— 「构建读到的源码」和「你写下的源码」不是同一份。
  改长度就能躲开（本批四个变异体里另外三个改了长度，全部正常变红），但那是运气不是方法。
  方法是：**变异台架每次注入之后、构建之前，先删掉 `__pycache__`**：

  ```bash
  find build -name __pycache__ -type d -exec rm -rf {} +
  ```

  一般形式：**「我改了源码」和「跑的是改过的源码」之间还隔着一层缓存**，而这层缓存
  在最像成功的那种情形下（改动小、改得快、长度没变）最容易命中。等长的编辑是变异验证的
  常态 —— 改一个数、翻一个比较符 —— 所以这不是边角情形，是这套方法的默认路径。
  **而作用域为 1 也可能改错了那一个。** `ENTRIES` 里大量字段在不同公司之间**逐字相同** ——
  `cost` 与 `mu` 的 `cadence_label` 当天都是 `"8 月底制财年；本站按自然年季度标注"`。
  一个会话要改 `mu` 那条,写了 `s.replace(old, new, 1)`,改到的是文件里更靠前的 **Costco**;
  作用域是 1、diff 是一行、`assert old in s` 也过了,而 Costco 拿到了一句「最接近 8 月 31 日
  的**星期四**」—— 它是**星期日**。它自己的断言只查 `mu`,照常通过。不带 count 的
  `replace` 更糟:两家一起改,同样全绿。**一个在文件里唯一的字符串,和一个在你关心的那条
  记录里唯一的字符串,是两回事。** 改法是把定位限制到那条记录(`"slug": "mu"` 与下一个
  `"slug"` 之间),断言该区间内恰好一次,并核对 `git diff` 的行数 —— 「只改了我该改的」
  本身就该是被量的东西,不是被相信的。
  **而量作用域的前提是操作发生过,所以还有更前的一层:先确认它能不能开始。** 同一天我跑
  一次 rebase 复核,`git rebase <目标>` 报 `fatal: invalid upstream`、退出码 128 —— 目标
  提交根本没进克隆的对象库 —— 而同一条命令后面的读数照常打印 `Ran 871 tests OK`、
  三数 `26 26 26`。**一个完全合格的通过,来自一次根本没发生的操作。** 根因很朴素:我把
  前一步 `git fetch` 的错误输出重定向掉了。三层次序是 ① 目标存在吗(`git cat-file -e
  <sha>^{commit}`)② 操作改了几个文件 ③ 读数属于哪棵树 —— 后两层都以前一层成立为前提。
  **不要把你的检查所依赖的那一步的错误输出丢掉。**
- **对照两个读数之前，先确认它们读的不是同一份东西。** 上面那条讲的是怎么**防住**，
  这条讲的是为什么**难以察觉**：注入 no-op 之后，你手里的"对照组"和"实验组"是同
  一份文本，于是两个读数必然一致，而那个一致读起来像是证据。同一形状的第三种外形
  在本节上面就有 —— **在抽取里先构建再核对摘要**，那一种连注入步骤都没有，assert
  无从下手，只能靠认出「这两个读数的输入是同一个」。三者是同一个错误的三张脸：
  两个正则跑同一份文本、构建后再和自己比、抽取里重新生成两边再互相比较。
  **共享同一个输入的两个读数，它们的一致不构成证据。**

  **别人的报告是一个输入，不是两个。** 并发会话里这条最常被绕过：拿同伴的描述和
  自己的假设互相印证，两者的共同输入是那份描述，于是印证必然成功。当天有会话据此
  给另一次事故分了错类 —— 它读的是叙述，没去核那个字符串在不在树上，而那一步才是
  独立的第二个输入。反过来，当天所有站住脚的交叉验证都有这个特征：拿到坐标之后用
  **自己写的脚本**重量一遍（`y=[-57.3, -50.5]` 被三方各自独立量到）、拿到「你的内容
  已在 main 上」之后自己跑 `git merge-base --is-ancestor` 确认、拿到一个数之后回
  申报文件重算。**同伴给的数字是线索，不是证据。**

  **而「自己写的脚本」只有在它不是照着对方描述写的时候才算第二个输入。** 2026-09-01
  接港交所时，对面为了自查「协调者那道闸门会不会标记我的图注」，**照我信里的描述重写了
  一份 `derivable()`**，跑出「零个被标记」。那个结论碰巧是对的，但它验证的是**它对我这道
  闸门的想象**，不是闸门 —— 它多塞了 `n−2` 和 `n+1` 两项（真实实现是 `{n, n−1, n−4}`
  加各 series 长度），只是恰好没碰到它关心的那个数。**它的重实现唯一的上游就是我的描述，
  所以拿它去验证我的闸门，必然一致。**

  这一张脸比前面几张难认，因为重实现**是不同的代码**，看起来最像独立验证：两边都吐出一个
  整数列表，都能拿去比对，甚至能得出正确结论。判据只有一条：**跑对方那份代码，不是跑你读
  完描述之后写的代码**——`import` 它、`git show` 它、在它的树上跑，三者任一都可以。
  报告这类自查结果时把这件事写出来：「我跑了你的闸门」和「我按你的描述重写了一段逻辑」，
  证据力差一个数量级，而两句话写出来一样短。

  **并行 agent 读同一份语料，也是一个输入。** 接 SK 海力士时：两个 agent 各自独立
  读完同一批 22 份季度业绩公告 PDF，都报「税前利润查无此项」，其中一个据此写了一整套
  「循环论证」的反驳。两个读数确实互不通气，但**语料是同一份**，而那份语料本身不含该项
  —— 存储三家的季度新闻稿只披露四行利润表（营收、营业利润、营业利润率、净利润），
  税前、税、营业外、capex、折旧、分部、量价一个都没有。那个数印在 6-K 的
  Preliminary Results 表上（税前 122,708,355 百万韩元，回原文核过）。**并发扇出会让
  「独立」这个词变得便宜**：agent 的数量不构成独立性，语料的不同才构成。派第二个
  agent 之前先问它读的是不是同一堆文件；答案是「是」的时候，它是复读不是复核。
  **独立性的表象越强，这张脸越难认出来** —— 那两个 agent 连论证方式都不同（一个直接
  说「查无此项」，另一个额外构造了一套循环论证），正是这种不同让「它们互相印证」显得
  可信。前三张脸靠形状就能认出来，这一张只能靠追问语料。

  **而「同一份语料」还能再细一层：同一份文档的不同**层**。** 2026-08-31 回填 SK 海力士
  2016–2020 时，两个 agent 各自独立读了同一批 20 份季度新闻稿，都报「2020Q4 净利润查无
  此项」「有五个季度没印营业利润率」。它们不通气、结论一致、连措辞都不同 —— 而两个都错。
  第三个 agent 是专门派去**反驳**它们的，它下载了随文那张业绩表图片：六个数全在图里。
  再往下还有一层：韩文域名 `news.skhynix.co.kr` 把同一张表嵌成 **HTML 文本**，28 季全有，
  精度还高一位。**「读的是不是同一堆文件」这个问法不够** —— 它们读的确实是同一批 URL，
  但只读到了文本层。问法要改成「你读到了这份文档的哪些层」：正文、随文表格（图片 / HTML）、
  附件、另一种语言的同一篇。

  **这一次真正的第二个输入是 DART**（법정 공시，与新闻稿完全不同的语料），它把 20 季里的
  15 季按独立三个月披露，45 个数字逐格对上。**判据不是「我派了几个 agent」，是「我拿到了
  几个互不派生的来源」** —— 而在派出去之前，先问一句「如果它读的层不对，我会不会看不出来」。
  这里的答案是不会：三次一致的错误读数看起来和三次一致的正确读数一模一样。
- **上一个基线上验过 ≠ 这个基线上成立。** 变异跑起来麻烦、结论又是二元的，做完
  一次就很想当成那条断言的固有属性。它不是，它是那条断言**在某一棵树上**的属性。
  rebase 之后连同变异一起重跑，**包括你认为「按理说不会变」的时候** —— 只在觉得
  可能出事时重跑，等于没有这条规矩。

  **但这条要有停止规则,否则它会变成另一种毛病。** 并发时 main 会因为纯文档提交反复
  前进,每来一个就挪一次、跑一遍,挣到的是同一份代码的第 N 个绿灯 —— 而**重复领取的
  绿灯和还没挣到的绿灯一样,会让人对绿灯本身脱敏**。分界不是「感觉变没变」,是**代码面
  证明为零**:

  ```bash
  git diff --stat <你验过的树> <新 main> -- ':!CLAUDE.md'   # 空 = 可以不跑
  ```

  - 代码面有差异 → 挪、全跑,**不论差异看起来多小**;
  - 代码面可证为零 → 不挪、不跑;
  - **推之前挪最后一次并全跑一遍**,不管中间积了多少文档提交。

  第三条是安全阀:任何被免掉的重跑最终都会被一次真实的重跑覆盖,所以前两条不必在
  「万一呢」上纠结。另外,「一分钟的测量胜过一段无懈可击的论证」只在**树变了、你想
  论证它等价**时成立;树没变时再跑一遍不是测量对论证,是重复。
- **skip 对上面那套数量算术结构上不可见。** 一个被跳过的测试**仍然计入 `Ran N`**，
  所以「基线 730 + 我的 1 条 = 731」在 render gate 根本没跑的情况下照样成立。数量
  能抓住 import 失败导致的静默掉测试，抓不住这个。**读 skip 数，不要读最后一行。**

  **而「读 skip 数」的那个显然写法是坏的。**2026-09-01 实测，`-v` 输出里
  `grep -ci "skipped"` 数到 **1**，真值（`TestResult.skipped`）是 **0** —— 匹配到的是
  一条测试 docstring 里的这个词。`tests/` 下有 **6 个**文件的正文含 "skipped"，而 grep
  只数到 1，因为 verbose 只打印 docstring 的**首行**：**这个读数取决于换行落在哪儿**。
  同一天另一个会话用同一条命令报「0 skip」，答案碰巧是对的 —— **而真阴那次没有任何东西
  会提醒你换仪器，所以它比假阳更危险**。两棵树上各量一次，把「无分辨力」变成了读数：

  ```
                        含 "skipped" 的 .py   verbose 里含该词的行   grep 读数   真实 skip
  本分支（1130 tests）          6 个                  1 行              1           0
  平行会话（1074 tests）        4 个                  0 行              0           0
  ```

  **同一条命令，在两棵都没有任何 skip 的树上给出不同的答案** —— 差别只在那个词落在各自
  docstring 的第几行。正确的量法不经过文本：

  ```bash
  python3 - <<'EOF'
  import unittest, io, sys
  sys.path.insert(0, '.'); sys.path.insert(0, 'tests')
  r = unittest.TextTestRunner(verbosity=0, stream=io.StringIO()).run(
      unittest.defaultTestLoader.discover('tests', pattern='test_*.py', top_level_dir='tests'))
  print(f"tests={r.testsRun} skipped={len(r.skipped)} failures={len(r.failures)} errors={len(r.errors)}")
  EOF
  ```

  同一节上面那条 `grep -c _FailedTest` 是同一个仪器。四棵树实测（同一份抽取，四份拷贝，
  各注入一处，注入前 `tests/` 下该字面量 0 处，四棵的 tests/skip/err 全部相同 1130/1/4）：

  ```
                                       grep -c _FailedTest   真实 load 失败
  ctl   未注入                                  0                  0
  注入到某 docstring 的第 3 行                   0                  0
  注入到某 docstring 的第 1 行                   1                  0    ← 假阳
  注入到测试方法名里                             1                  0    ← 假阳
  ```

  **所以不是「写进 `tests/` 就会」——是「落在 docstring 首行或方法名里才会」**，因为
  verbose 只打印这两样。写在 docstring 正文里是安全的，这条值得说准：说成前者，读的人
  会以为只要不提这个词就安全，于是连有用的记录也不写了。

  **两条 grep 都只会假阳，不会假阴**（也实测了：往一个测试文件追加一行非法语法，真实
  load 失败 1，`grep` 数到 2；真实 skip 1，`grep -ci skipped` 数到 3）。**grep ≥ 真值，
  永远不会低于。**所以危害不是放过事故，是**发明事故** —— 然后在你查清它是假的之后，把
  「这条闸门爱乱叫」变成不看它的理由，也就是 §6 结尾那条「一个会误红的闸门会被
  `--no-verify` 绕过，然后什么都不保护」。而真阴之所以更险，是同一条链的另一端：一个
  正确答案让你**没有机会**发现它爱乱叫。

  最后一层是自指：这条闸门的靶字符串还没进语料，**而最可能写下它的正是记录本次事故的
  那条 docstring** —— 它会在有人正确使用这个仓库的习惯（把事故写进 docstring）时失效。
  这和 §3 数会话名那条、以及
  「指引正则读到同段末尾的 capital budget 句」是同一族：**一个把「讲这件事的文字」算进去
  的计数，对它要回答的问题按构造无分辨力**；而匹配失败会报 `None`、匹配错句会报一个
  很像样的数，后者不会让任何人停下来。
- **一次红不构成证据，除非你知道是什么把它变红的。** 上面每一条防的都是**假绿**；这一条
  是反方向,而它同样会让整套变异验证失去意义。台架普遍把 `returncode != 0` 读成「闸门
  响了」,但非零可以来自任何原因。实测过:往 `build/board.py` 末尾追加一行非法语法,再跑
  一条与它毫无关系的断言,台架照样打印 **RED** —— 而那条被瞄准的断言**根本没有运行**,
  失败列表里连它的名字都没有(载入就失败了)。同一天另一个会话踩到的是更隐蔽的版本:
  上一个变异体留下的生成物让**别的**断言失败,于是它把一次无关的红收下当作证据,而那条
  它以为守住了的闸门其实一直有洞 —— 那个洞是「scope 正则拿语气助词当键」,它后来把
  文案里的「季里」改成了「季，」,整句从此不进扫描。而这两个洞的性质不同:台架那个洞**一直
  在那里**,任何一次严格重跑都会发现;正则那个洞是**改文案的那一刻才产生**的,在那之前它
  是对的。所以它不是「写错了」,是**一个曾经正确的键在别处的编辑下失效** —— 没有任何时刻
  会报错,也没有任何 diff 看起来可疑:那个提交里测试是绿的、文案是对的,错的是它们之间
  那条没人看得见的耦合。**键落在措辞上,就等于把一条断言的有效性外包给了以后所有编辑
  那段文字的人。** 键要落在结构上(「这个数后面跟的是不是判词」),不落在措辞上。

  **每个变异体要声明它该打中哪条断言,只有那条自己出现在失败列表里才算 RED**;打中别的
  算 `WRONG TEST`,和 GREEN 一样是待查而不是通过。**还原源码之后必须重建**,否则下一个
  变异体会继承上一个的生成物、因为别人的原因变红。本文档此前记录的所有变异结论都是在
  宽判据下得出的,已按严格判据重跑,8/8 确实是被瞄准的那条断言抓住的 —— 结论没变,但
  那是重跑挣来的。

  和「连提交信息里的数都要重算」是同一族:**都是没有闸门看着的地方,而它们恰好是闸门
  本身的可信度所依赖的地方。**

```bash
python3 - <<'PY'
import hashlib, pathlib, re, sys
root = pathlib.Path(sys.argv[1] if len(sys.argv)>1 else ".")
bad = n = 0
shells = sorted(root.glob("*/index.html"))
for s in shells:
    for m in re.finditer(r'src="\.\./(\S+?)\?v=([0-9a-f]+)"', s.read_text(encoding="utf-8")):
        n += 1
        if hashlib.sha256((root/m.group(1)).read_bytes()).hexdigest()[:len(m.group(2))] != m.group(2):
            bad += 1; print("DRIFT", s.parent.name, m.group(1))
print(f"{n} script tags / {len(shells)} shells / {bad} drifted")
PY
```

---

**还有一张脸不在语料上，在推理上：一个文件系列里的规律，被当成了对全部申报的穷举。**
2026-08-31 发现 AmEx 页把 2016 那四季留在 ASC 606 之前的旧口径上，理由写在核对表里：
「2016 从未在统计表里被重述过，因为每份发布只并排印五个季度，最后一份带 Q4 2016 的发布
早于该变更。」前半句是观察，**后半句是把「业绩发布」这一个文件系列上的规律，
当成了公司全部申报的穷举**。实际 AmEx 在 2018-03-09 furnish 了一份 Item 7.01 的 8-K，
Exhibit 99.1「Eight Quarter Trend Q1 2016 through Q4 2017」把 As Reported / Adjustments /
As Recast 三栏并排印了八个季度 —— 它不是业绩发布，所以那条规律对它按构造无效。

**这个形状比「没查」更难认，因为它带着一条理由，而理由本身是真的。** 判据是问一句：
这条规律是在哪个文件系列上观察到的？我的结论是关于**那个系列**，还是关于**这家公司**？
两者之间隔着「它还会不会用别的形式发同一件事」——8-K/6-K 的 Item 7.01、临时公告、
招股书、投资者日材料、另一种语言的同一篇，每一样都不在「季度业绩发布」这个系列里。

**而这类命题一旦发布出去，就不能靠重算发现了 —— 要靠数量词去找。** 2026-09-01 接港交所时，
一个会话的整页中心论断被推翻后做了一次普查：不扫数字，扫**全称与否定量词**（从来／从未／
唯一／只有／没有任何／全部／不存在），33 条逐条回查来源，抓出三条已发布的假命题。三条里
两条是**陈旧**（写的时候对，后来公司改了披露），第三条是**量词错**（写的时候就错）——

```
陈旧    「六项费用收入从未按季印过」        FY2022 起假：年报那张季度表当年加了这几行
陈旧    「对照唯一覆盖到的三条线之一」      对照后来扩到 11 个科目
量词错  「本窗口内唯一一次重分类」          发布当天就错：把「本页比对的 1091 对读数里
                                          只有这一处」升格成了世界的性质
```

**前两种任何一次重新计数都会撞见；第三种不会，因为它的反例按定义在你数的那堆东西之外。**
所以判据不是「再数一遍」，是问「**这个全称命题的语料边界在哪**」—— 它是关于我读过的那批
文件的，还是关于这家公司的？§5 上面那条讲的是你**推理**时越界，这一条讲的是越界之后
**它已经印在页面上了**，而任何在原语料上重跑的检查都会继续说它是对的。

同一天还有一个直接的推论：**一条豁免／一个 pin 的「理由」也是被发布的陈述，适用同样的举证
标准。** 当天我给一条 pin 拟的理由里写着「本页没有任何一张图画出那 13 格」—— 平行会话逐图
量了 x 轴：八张 42 季图**每一张都画着那 13 格**，那半句是假的。真正的原因也不是它：是
`13 = 42 − 29` 里的 42 是**别的图**的轴长，而那道闸门按构造是逐图的（该图自己的可推集合只有
`[25, 28, 29]`）。一句听起来很像理由的话，和一句被量过的理由，写出来长得一样。

**顺带记一条便宜的量法：当一份文件同时覆盖你已有的区间和你要取的区间，重叠区间就是
不确定性的量尺。** 那份 8-K 通篇标着 (Preliminary) 且从未被最终版取代 ——
听起来只能承认风险。但它同时印了 2017，而本页早就用了 2017 的最终值：
对着四个 2017 季度，它在收入差 1 百万、拨备差方向相反的 1 百万，
在总费用、税前、净利润上一分不差。**「Preliminary 有多不准」于是从形容词变成了数字。**

---

## 6. 绿测试不等于图画出来了

整套验证栈检查的是 **payload**，从来不看**图**。`payload_guard` 拒绝 payload 里的
非有限值，各家的测试断言数字和形状，`build/all.py` + `git status` 查漂移 —— 三者
都会被一个画出坏图的 payload 满足，因为**渲染器内部算术产生的 NaN 对它们不可见**。

两个已经踩到的真实例子：

- `kind: "gs_bar"` 不给 `yoy` 时会画一条 `Y(ex.avg12)` 的虚线基准线，而 `avg12`
  是**由 payload 提供**的、不是渲染器算的。全站 27 个 `gs_bar` 里 26 个给了 `yoy`、
  **0 个给了 `avg12`**，所以这条分支从来没被真实数据走过；唯一两者都没给的
  AVGO Ex16 输出 `<line y1="NaN" y2="NaN">`，浏览器静默丢弃，线根本没画出来。
- 八季序列上的十二期移动平均同样是 NaN。
- `kind: "stacked_dual"` 的右轴上界**写死在 60**（`charts.js:914`
  `rtk = ticks(0, rc.ymax || 60, 6)`，不看数据）。超过 60 的线被画到负 y、浏览器
  直接裁掉，而图例照常写着它。`ibkr` Ex8（少数股东占比 77.9%）就这样发了很久：
  实测坐标 `y=[-57.3, -50.5]`，画布高 `332.2`。**这一处一个 NaN 都没有** ——
  坐标全是合法有限数字，只是落在画布外，所以只查 NaN 的那套断言按构造看不见它。
  画占比线时传 `"ymax": 100` —— **传在 `ex.line` 里,不是 exhibit 顶层**。`rc = rhsOf(ex)`
  返回的就是 `ex.line`(`gs_bar` 则是 `ex.yoy`),所以顶层写 `"ymax"` **静默无效**:没有报错、
  没有警告,轴照样落回 60。main 上现存 9 张 `stacked_dual` 全部写在 `line` 里,但那是惯例
  不是文档 —— 这一条是接第三批时有人照文档写在顶层、变异之后才发现的。其余 dual 图型走
  `ticks(min(rv,0), max(rv), 9)`,按数据算,不受影响。

  **顺带记两处「恰好没事」的:** `ibkr Ex5`(净利息占比,峰值 **58.79**)与 `pm Ex17`
  (无烟产品占净收入,峰值 41.46)两张 `stacked_dual` **一个 `ymax` 都没传**,今天不出事
  只因为都在 60 以内 —— `ibkr Ex5` 离那道坎只剩 1.2 个百分点,再涨一个季度就会静默消失。
  这正是本节结尾那句问自己的话:没出现的那些次,是因为有东西挡着,还是因为恰好。

**比「要不要渲染」更难的是「渲染之后断言什么」。** MCO 那次少一根柱的作者当晚
**确实**开了浏览器 —— 它查了 NaN 属性、字面标签、SVG 数量，全是它前一小时刚被烧过
的失败模式；它没查「每张图的柱子数是不是等于它自己 x 轴承诺的数目」。少一根柱不留
NaN、不抛错、不产生空元素、数值还全对，三条断言按构造都看不见。**浏览器检查是对的
直觉，断言集选错了。**

一套只检查上次失败模式的渲染检查，和一套只检查 payload 的测试，是同一种局限，只是
换了一层楼。**断言要从「这张图自己承诺了什么」推导，不要从「上次是怎么坏的」推导** ——
`len(values) == len(xlabels)`（标签数 == 柱子数）、纵坐标落在 viewBox 之内，这类
结构恒等式能抓到你还没见过的那一类；失败模式清单不能。

**而闸门要活下来，靠的是它拒绝报警的地方。** 越界检查只对 `fill="none"` 的
path/polyline 生效、且只查纵轴：横向不查，因为 end_label 与柱标本来就故意画在边距
里；`fill` 不为 none 的不查，因为堆叠面积块本来就贴边。同理，NaN 要用 token 边界
而不是 `includes`（否则匹配到 IR 链接里 "Fi**nan**cial" 的中间），README 的 bullet
要锚定行首（否则吃掉正文里的行内地址），公司名匹配要双向包含而不做后缀剥离。
**一个会误红的闸门会被 `--no-verify` 绕过，然后什么都不保护** —— 这句话 2026-08-29
当天被四个互不通气的会话各自独立说了一遍，它是这一整节的前提，不是脚注。

**最后，闸门存在的理由不是「出过事」，是「躲开是靠运气」。** 写死 60 那条被发现的
当天，同时接入的另一个交易所页没有中招，它自己的复盘是：

> 我最后没中招，但那是因为唯一的 `stacked_dual` **恰好**传了 `ymax`，
> 不是因为我知道有这回事。

这是「没出事」和「不会出事」的全部区别。本节六条闸门对应的每一个缺陷，在被抓到
之前都有人这样躲过去过 —— `ibkr` Ex8 躲了很久没躲掉，那一页躲掉了却不知道自己在躲。
所以判断一条闸门值不值得存在，不要问「这个错误出现过几次」，要问「没出现的那些次，
是因为有东西挡着，还是因为恰好」。

**还有一个理由,比上面那个更难接受:一个刚被诊断出来的形态,不会因为你诊断过就绕开你。**
接存储三家当天,同一个会话在几小时内连中三次自己刚讲过的东西:它为自己那一页写了计数
闸门、变异之后又把闸门从标题扩到图注,**仍然漏掉 brief 里同一个计数的第三份副本**
(`bbfd652`);它认出并修掉了自己页上一个与 mco Ex9 同形的空列(一条恰好为零的分解腿),
随后用裸列表的 `net` 在**同一张图**上造出另一个空列(`aaf3ea8`);它指出「README 里的
计数会随下一份 10-K 腐烂」,半小时后把一个同样的计数写进自己的 subtitle。我这边同期:刚把「先量作用域再读闸门」写进 §5,就跑了一次 `fatal:
invalid upstream` 之后照常打印 `Ran 871 tests OK` 的复核;刚写完「一次红不构成证据」,
下一轮的第一条变异就打偏了靶子。

**共同点不是粗心,是把「我理解了这个形态」当成了「我不会再犯」。** 理解不是防护,它只是
让你在事后认得出来 —— 而认得出来的前提是有东西把它抓住。这就是本节所有闸门的理由:
**它们防的不是不懂的人,是刚刚讲完这件事的那个人。**

**推之前把页面渲染一遍。** 无人值守的会话里 `preview_start` 会被直接拒绝，
也不能用 `file://`（shell 靠相对 `<script src>` 加载 payload）——**不要绕过这个拒绝**，
改用 jsdom：把 `npm install jsdom` 装到临时目录（**不要装进仓库**），跑真实渲染器：

```js
const fs = require("fs"), { JSDOM } = require("jsdom");
const REPO = process.argv[2];
const SLUGS = fs.readdirSync(REPO).filter(d =>
  fs.existsSync(`${REPO}/${d}/index.html`) && fs.existsSync(`${REPO}/data/${d}.js`));
const NANRE = /(^|[^A-Za-z])(NaN|Infinity|undefined)([^A-Za-z]|$)/;
for (const slug of SLUGS) {
  const errs = [];
  const dom = new JSDOM(fs.readFileSync(`${REPO}/${slug}/index.html`, "utf8"),
                        { runScripts: "outside-only", pretendToBeVisual: true, url: "https://x/" });
  dom.window.console.error = (...a) => errs.push("console.error: " + a.join(" "));
  try {
    ["data/roster.js", `data/${slug}.js`, "assets/charts.js", "assets/page.js"]
      .forEach(r => dom.window.eval(fs.readFileSync(`${REPO}/${r}`, "utf8")));
    dom.window.document.dispatchEvent(new dom.window.Event("DOMContentLoaded", { bubbles: true }));
  } catch (e) { errs.push("threw: " + e.message); }
  const d = dom.window.document;
  for (const el of d.querySelectorAll(".grid svg *"))
    for (const a of el.getAttributeNames())
      if (NANRE.test(el.getAttribute(a))) { errs.push(`NaN attr ${el.tagName}.${a}`); break; }
  const empty = [...d.querySelectorAll(".grid > *")].filter(n => !n.querySelector("svg")).length;
  if (empty) errs.push(`${empty} grid children without svg`);
  // notes 走 esc()：这里出现字面标签就是读者看得见的字符
  const lit = [...d.querySelectorAll("li")].map(li => li.textContent)
                .filter(t => /<\/?[a-z][a-z0-9]*>/i.test(t));
  if (lit.length) errs.push(`literal tag in ${lit.length} <li>`);
  if (!d.querySelector("optgroup option")) errs.push("not in nav dropdown");
  console.log(`${errs.length ? "FAIL" : "OK  "} ${slug}` + (errs.length ? "  " + errs.join("; ") : ""));
}
```

检查器自己也有一个坑：**NaN 要用词边界判，不能用 `includes`。** 一个天真的
`/NaN|Infinity|undefined/i` 会匹配到 NVIDIA 那条 IR 链接里 "Fi**nan**cial" 的
中间，报出 26 个幻觉失败。

---

## 7. 渲染契约：哪些槽会把标记当字面字符印出来

`assets/page.js` 的分工是：

- `headline` / `title` / `subtitle` / `tracker` 用 `textContent` 写入；
- `section.title` / `section.description` / **`notes`** / 表格标题走 `esc()`；

以上四类里的 `<b>` 都会变成读者看得见的六个字符。
而 `brief` / `footer` / 每个 exhibit 的 `note` / `src_extra` 是原样 innerHTML，
那里的标记是合法且大量使用的（全站 140 多条图注用了）。

`test_literal_text_fields_carry_no_markup`（`tests/test_content_boundary.py`）
钉住这条。注意它**对 `v` / `ibkr` / `mco` / `spgi` 无法变红** —— 那四个 builder
把 notes 建成 `[plain_text(p) for p in [...]]`，标记在进 payload 之前就被剥掉了，
它们靠构造受保护而不是靠这条断言。绿灯不等于那四个 builder 的源码里没有标记。

写 note 的时候直接不写标记，比包一层 `plain_text()` 好：`plain_text()` 的立论是
"同一句话既要进图注又要进 notes，写一遍带标记再剥"，没有这个前提就只是在源码里
留下永远不生效的标记，还让闸门对那个 builder 永远无法变红。

---

## 8. 读提交标题时注意

这个仓库每条提交标题陈述的是**它修掉的那个发现**，不是它留下的状态。
`并入 SPGI：…13 张卡配着 12 的计数` 读起来像一棵坏掉的树，实际上是那次修复。
看树，别看句子。

**写的时候有对称的一条:关于自己做过什么的陈述,和关于代码的陈述,适用同一条举证标准 ——
而前者最不容易想到要去查。** 接存储三家当天,一个会话在复盘里写「我修了 mco 那个空列」,
它实际做的是「在自己页上认出并修掉了一个与 mco Ex9 同形的空列」。mco 那根柱子是
`bff9deb` 修的,12:13,**早于那一批的基线**,而那一批没有任何提交碰过 `build/mco.py`
(`git log <基线>..<顶端> -- build/mco.py` 为空)。它关于 mco 的全部知识来自开工时读到的
记忆条目和 `test_chart_contract.py` 的 docstring —— **「我读到过这个案例」被压缩成了
「我处理过这个案例」,而这两者在自述里长得一样。**

我照收了那句话、写进了正文,只在提交信息里注了一句「未复现」。**「未复现」不是一个能让
陈述句降级的标记:要么去核,要么别把它写成事实。** 三条都便宜:`git log --format='%an %ad'
<sha>`、`git merge-base --is-ancestor <sha> <你的基线>`、`git log <基线>..HEAD -- <文件>`。

这一族和 §6 那条不同 —— 那条讲的是理解了形态仍会复现它,靠闸门防;这一条是**记账**:
在数自己的战绩时把别人的一笔算了进来。今天对树上每一个数字都要求了度量,唯独对
「我做了什么」用了记忆。
