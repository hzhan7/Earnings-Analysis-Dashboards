# 错题本：在 GitHub 上做数据网页时，那些绿灯照亮的坑

这份不是清单，是**判据**。每一条都在 2026-08-29 到 09-01 之间被真实踩到过，
形状按「**为什么当时没有任何东西变红**」分类 —— 因为凡是会变红的错，都不必写下来。

站点形态：静态页 + 手写构建脚本生成 `data/*.js` payload + 一个共享渲染器
（`assets/charts.js`）+ shell 用内容哈希 `?v=` 引用 payload。下面的坑大多与这个形态绑定，
换成任何「脚本产出数据、前端画图、GitHub Pages 托管」的项目都成立。

---

## 一、渲染层：整套测试都不看图

**这一节的前提**：payload 守卫、单元测试、`build && git status` 三者**都会被一个画出坏图的
payload 满足**，因为渲染器内部算术产生的东西对它们不可见。

### 1. 图从不标最后一格 —— 而那一格正是标题在讲的那个

刻度抽稀的循环从下标 0 正着走（`for (i = 0; i < n; i++) if (i % step) continue`），
于是末格只有在 `(n-1) % step === 0` 时才落在刻度上。实测 198 张设了 `xstep` 的图里
**196 张**不满足。每张图的标题、end label、页面 headline 讲的都是最新那个数据点，
而 x 轴上恰恰没有它。

**为什么没被抓住**：不产生 NaN、不抛错、不留空元素、数值全对。

**尤其危险的地方**：`xstep` 只有长轴才需要 —— 所以「把窗口从 8 期拉到 42 期」这个动作
**本身就是把页面推进这个缺陷的动作**。做得越多，这个数越大，不会自己收敛。

**判据**：末端锚定步进（从 `n-1` 倒着走），再把最小的那个下标换成 0 保住起点。
闸门断言「每张图必须为它自己的最后一个数据点画出刻度标签」。

### 2. payload 里拼错的键，渲染器静默丢弃

右轴标题的键是 `ylab2`。有 6 个页面拼成了 `rlab` / `rhs_label` —— 两种拼法渲染器都不读，
**20 条右轴标题直接消失**，而构建脚本源码里看起来是标了的。

**更糟的一层**：右边距也是按正确的键算的（`ex.ylab2 ? 56 : 42`），所以拼错的后果不是
「标题没画出来」，是**连给标题留的位置都没留**，刻度挤在画布边上。

**判据**（一般形式，不针对某个拼法）：
> **全站每个 payload 键都必须在某个消费者的源码里出现。没有消费者读的键，是看起来像活的死配置。**

把它写成普查而不是写成「修 rlab」，是找到另外 18 处的唯一原因。

### 3. 坐标合法，但在画布外

某图型的右轴上界写死 60（`ticks(0, ex.line.ymax || 60, 6)`，不看数据）。超过 60 的线
被画到负 y，浏览器直接裁掉，**而图例照常写着它**。反方向同理：零起点的轴上出现负值，
一样飞出画布。

**这一处一个 NaN 都没有** —— 坐标全是合法有限数字，只是落在画布外，所以只查 NaN 的断言
按构造看不见它。

**判据**：查「描边路径的纵坐标是否落在 viewBox 内」。注意豁免要精确：只查
`fill="none"` 的 path/polyline、只查纵轴（横向的 end label 本来就故意画在边距里）。
**一个会误红的闸门会被绕过，然后什么都不保护。**

### 4. 渲染器内部算出来的 NaN

payload 里每个值都有限，渲染器一做算术就 NaN（例如引用一个 payload 从不提供的字段）。
浏览器**静默丢弃**带 NaN 几何属性的元素：没有 console 错误、没有空白卡片、没有布局位移。
图看起来是完成的。

**判据**：jsdom 跑真实渲染器，扫 SVG 属性与文本。

### 5. 检查器自己的坑：`NaN` 要用词边界

天真的 `/NaN|Infinity|undefined/i` 会匹配到 "Fi**nan**cial" 的中间 —— 页面上的
IR 链接全中，报出一堆幻觉失败，然后这条检查会被删掉。

---

## 二、被印出来的数字：重数，不要加一

### 6. 增量法的输入里包含它要检查的那个数

首页的「N 家公司」、图注里的「N 个季度」、README 的家数、提交信息里的测试条数、
测试文件里写死的普查字面量 —— 每一个都是手打的，每一个都会有人在旧值上 +1。

**最危险的是它不冲突的时候**：两个分支各自在自己的基线上都数对了，写的是**同一个数**，
git 干净合并这一行、同时合并两边的新增内容 —— 结果 N+1 个东西配着 N 的计数，
任何地方都没有标记。

**判据**：从源头现算（`len(ENTRIES)`），并交叉核对（`grep -c` 结构标记）。
**心里预存一个终值（"应该是 27"）同样是增量法。**

### 7. 一个计算出来的数被印了不止一次

同一个数字会同时出现在标题、图注、subtitle、README、提交信息里。修了两处，第三处留着。

**判据**：让文案**读**那个计算结果，不要写字面量；数不清有几份副本时，先做副本普查。

### 8. `grep -c` 把"讲这件事的文字"也算进去

数 skip 数用 `grep -ci "skipped"` → 数到 1，真值是 0，匹配到的是一条测试 docstring 里的
这个词。而且 verbose 只打印 docstring **首行**，所以**这个读数取决于换行落在哪儿**。
同一条命令在两棵都没有 skip 的树上给出 0 和 1。

**判据**：不经过文本 —— 直接读结构化结果（`TestResult.skipped`）。
一般形式：**一个把「讲这件事的文字」算进去的计数，对它要回答的问题按构造无分辨力。**

---

## 三、数据口径：算术全对，意思是错的

### 9. 口径拼接（basis splice）

一条时间序列里接了两个不同的披露口径（口径切换、重分类、准则变更、分部重划）。

**为什么求和恒等式永远抓不到**：重分类只在各科目之间搬钱，所以「分项相加 = 合计」
在两套口径下**都成立**。审计过的 7 个页面里 **6 个**有这个缺陷，全部通过了当时 1121 条测试。

**判据**：对每条序列问一句「这 N 格是不是同一个披露总体？」凡是跨过口径变更的，
在数据文件里显式记一个口径块，并写一条**能看见该类缺陷的**断言 —— 求和恒等式不算。
公司改口径时通常在表底加脚注，而那条脚注不进任何结构化数据源。

### 10. 按整数下标比较两个起点不同的数组

拿 42 格（2016 起）对 38 格（2017 起）逐下标比，"证出"一个 16/16 精确的错位 ——
纯属虚构。**按期间标签对齐，永远不要按下标。**

### 11. 判据格上两个候选答案相同

为了判定该用哪个数据源，挑了一个格子去核 —— 而那一格两个源印的是同一个值，
**它什么也没判定**。选判据之前先确认：在这一格上，两个候选答案是不是不同的。

### 12. 同一份文档的不同「层」

正文 / 随文表格（图片或 HTML）/ 附件 / 另一种语言的同一篇。两个 agent 各自独立读同一批
文件、都报「查无此项」、措辞还不同 —— 两个都错，那些数在随文的**表格图片**里。

**问法不是「你们读的是不是同一堆文件」，是「你读到了这份文档的哪些层」。**

### 13. 一个文件系列上的规律，被当成对全部来源的穷举

「每份季度发布只印五个季度，所以从未被重述过」—— 前半句是观察，后半句把一个**文件系列**
上的规律当成了这家公司的性质。实际另一种文件类型里就并排印着八个季度。

**判据**：这条规律是在哪个文件系列上观察到的？我的结论是关于**那个系列**，还是关于
**这个对象**？两者之间隔着「它还会不会用别的形式发同一件事」。

### 14. 已发布的全称命题，重算永远发现不了

「从未 / 唯一 / 只有 / 没有任何 / 全部」这类量词一旦发布出去，就不能靠重新计数发现错误 ——
**它的反例按定义在你数的那堆东西之外**。

区分两种：
- **陈旧**：写的时候对，世界变了 → 任何一次重新计数都会撞见；
- **量词错**：写的时候就错 → 重算永远撞不见。

**判据**：定期扫**量词**而不是扫数字，逐条回查「这个全称命题的语料边界在哪」。
一次这样的普查在一个页面上抓出三条已发布的假命题。

### 15. 精度混层，而数小数位看不出来

同一列数字，一部分取自正文抬头句（2 位小数），一部分取自表格（3 位小数）。
用「数小数位」去做分层普查，**在末位为 0 的值上按构造无分辨力**（JSON 把 `203.50`
序列化成 `203.5`），于是普查结果是个下界，连缺口位置都可能是假的。

---

## 四、验证方法本身会骗人

### 16. 变异验证的四层，缺一层就拿到假绿

1. **注入要自带断言**：`str.replace` / `sed` 找不到目标时**静默返回原文**，于是你在对一个
   没被改过的文件跑闸门，拿它的绿灯当证据。写 `assert old in s`。
2. **事后量作用域**：改了几个文件 / 几处，非零才继续。有些操作没有 old 可 assert
   （例如「把某个目录还原」），前置断言救不了，只有事后量。
3. **注入之后、构建之前清 `__pycache__`**：等长编辑 + 同一秒写入会让 pyc 不失效 ——
   源码写着 42、生成物里还是 60、整套测试全绿。**等长编辑是变异验证的常态**（改一个数、
   翻一个比较符），所以这不是边角情形。
4. **每个变异体声明它该打中哪条断言**，只有那条自己出现在失败列表里才算 RED。
   打中别的算 `WRONG TEST`，和 GREEN 一样是待查。

### 17. 一次红不构成证据，除非你知道是什么把它变红的

`returncode != 0` 可以来自任何原因，包括那条被瞄准的断言**根本没运行**（载入就失败了）。
上一个变异体留下的生成物也会让**别的**断言失败。**还原之后必须重建。**

### 18. 对照组：两边一致的红，和两边一致的绿一样，不构成证据

跑变异时对照组和实验组失败得一模一样 —— 拷贝树漏了几个目录，套件只载入了应有数量的
一小部分。那一轮什么都没测到。**每次变异跑都要有一个已知为绿的对照组。**

### 19. 共享同一个输入的两个读数，它们的一致不构成证据

已经踩到的几张脸，从易认到难认：
- 两个正则跑同一份文本；
- 构建之后再和自己比（**在抽取里先构建再核对摘要，无论提交了什么都会通过**）；
- 同伴给的描述 + 你自己的假设互相印证；
- 并行 agent 读同一批文件（**agent 的数量不构成独立性，语料的不同才构成**）；
- **照着对方描述重写的检查器，用来验证对方的检查器** —— 它唯一的上游就是那份描述，
  所以必然一致。这张脸最难认，因为重实现**是不同的代码**，看起来最像独立验证。

**一般形式：一个从被检查对象推导出自己期望值的检查，不可能失败。**
更早的探测信号是：**一个你还没挣到的绿灯。**

### 20. 前置层：先确认操作发生过

`git rebase <目标>` 报 `fatal: invalid upstream`、退出码 128，而同一条命令后面的读数
照常打印 `Ran 871 tests OK`。**一个完全合格的通过，来自一次根本没发生的操作。**
根因很朴素：前一步 `git fetch` 的错误输出被重定向掉了。

三层次序：① 目标存在吗 ② 操作改了几个文件 ③ 读数属于哪棵树。后两层都以前一层成立为前提。
**不要把你的检查所依赖的那一步的错误输出丢掉。**

### 21. 改树的操作单独一条命令，读数另起一条

把改树和读数混在同一条 shell 命令里，就没有任何东西替你记住「这些读数属于操作之后的
那棵树」。恶劣之处不是读数变错了，**是读数仍然自洽** —— `25 25 25` 是一个完全合格的通过。

---

## 五、Git 与发布

### 22. 逐字节相同不是共同祖先

cherry-pick 造的是新提交，两条分支**没有包含那些改动的共同祖先**。那些文件仍然干净合并，
是因为 3-way merge 的「两边做了同一件事」：base 有 X、两侧都是 Y，取 Y。
**是「逐字节相同」在替代共同祖先干活**，它一旦被任一侧的一次编辑打破就退化成冲突。

**判据**：不要从「内容相同」或「谁改哪个文件」预测冲突。
`git merge-tree --write-tree --name-only A B`，**在候选编辑前后各跑一次**。

### 23. 内容哈希 `?v=` 与生成物

shell 用 `sha256(payload)[:8]` 引用 payload，所以**任何动了 payload 的提交都必须带上
所有链接它的 shell**；动了全局 roster 的提交要带上全部 shell。

两个真实走法都中过招：
- `git commit --only <路径>` 会**排除你自己重新生成的文件**；
- `git add` 之后又跑了一次构建 —— 生成物变了却没重新暂存，提交里带的是构建前的版本。

**判据**（提交之后，从提交本身抽取，两道检查形式相反）：
- **import 检查，要构建**：抓「构建脚本 import 了一个文件却没被跟踪」；
- **摘要检查，不要构建**：直接核对每个 `?v=` 与它指向的那个**已提交**文件的哈希。
  在抽取里先构建再核对，等于把两边都重新生成再互相比较，无论提交了什么都会通过。

### 24. 授权的两条边界

- **一次授权是对一份具体内容的授权，不是对一个分支名的授权。**内容的立论换掉之后，
  先前那次批准就不再覆盖现在这棵树。
- **一次授权是对一个执行者的授权，不是对一件事的授权。**「把这件事交给 A」让 A 可以做，
  不代表 B 被授权做同一件事，也不代替 B 那边的那一次。
- **排程 ≠ 授权。**「轮到你了」不等于「你被授权了」。

---

## 六、最后三条，关于这份文档本身

**闸门存在的理由不是「出过事」，是「躲开是靠运气」。**判断一条检查值不值得存在，
不要问「这个错误出现过几次」，要问「**没出现的那些次，是因为有东西挡着，还是因为恰好**」。

**断言要从「这个东西自己承诺了什么」推导，不要从「上次是怎么坏的」推导。**
`标签数 == 柱子数`、`纵坐标落在 viewBox 之内`、`每张图必须标它自己的最后一格` ——
这类结构恒等式能抓到你还没见过的那一类；失败模式清单不能。

**理解不是防护。**本轮里，同一个会话在写完「先量作用域再读闸门」之后立刻跑了一次
`fatal: invalid upstream` 却照常打印 OK 的复核；在写完「重数不要加一」之后
把一个 117 的普查读成了 117+4。**这些闸门防的不是不懂的人，是刚刚讲完这件事的那个人。**
